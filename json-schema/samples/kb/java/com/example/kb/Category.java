// Generated by nex-gen. DO NOT EDIT!

package com.example.kb;

import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.JsonDeserializer;
import com.fasterxml.jackson.databind.JsonNode;
import com.fasterxml.jackson.databind.JsonSerializer;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Objects;
import org.jspecify.annotations.NullMarked;
import org.jspecify.annotations.Nullable;

/**
 * A node in a self-recursive category tree. Closed struct. The {@code
 * children} self-reference is a within-file cycle; in Java it is free — a
 * bare {@link Category} object reference, no forward-declaration needed.
 */
@NullMarked
@JsonDeserialize(using = Category.Deserializer.class)
@JsonSerialize(using = Category.Serializer.class)
public final class Category {
  private final String id;
  private final String name;
  private final @Nullable List<Category> children; // optional; recursive self-reference

  public Category(String id, String name, @Nullable List<Category> children) {
    this.id = id;
    this.name = name;
    this.children = children;
  }

  public String getId() {
    return id;
  }

  public String getName() {
    return name;
  }

  /** Sub-categories, if any. */
  public @Nullable List<Category> getChildren() {
    return children;
  }

  public static final class Deserializer extends JsonDeserializer<Category> {
    @Override
    public Category deserialize(JsonParser p, DeserializationContext ctx) throws IOException {
      JsonNode root = p.readValueAsTree();
      List<Violation> errs = new ArrayList<>();

      String id = null;
      JsonNode idNode = root.get("id");
      if (idNode == null || idNode.isNull()) {
        errs.add(new Violation("id", "required"));
      } else if (!idNode.isTextual()) {
        errs.add(new Violation("id", "expected string"));
      } else {
        id = idNode.asText();
      }

      String name = null;
      JsonNode nameNode = root.get("name");
      if (nameNode == null || nameNode.isNull()) {
        errs.add(new Violation("name", "required"));
      } else if (!nameNode.isTextual()) {
        errs.add(new Violation("name", "expected string"));
      } else {
        name = nameNode.asText();
      }

      // children: optional array of Category (recursive self-reference).
      List<Category> children = null;
      JsonNode childrenNode = root.get("children");
      if (childrenNode != null && !childrenNode.isNull()) {
        if (!childrenNode.isArray()) {
          errs.add(new Violation("children", "expected array"));
        } else {
          children = new ArrayList<>();
          for (int i = 0; i < childrenNode.size(); i++) {
            JsonNode item = childrenNode.get(i);
            try {
              children.add(ctx.readTreeAsValue(item, Category.class));
            } catch (ValidationException nested) {
              for (Violation v : nested.getViolations()) {
                errs.add(new Violation("children[" + i + "]." + v.getPath(), v.getReason()));
              }
            }
          }
        }
      }

      Iterator<String> it = root.fieldNames();
      while (it.hasNext()) {
        String k = it.next();
        switch (k) {
          case "id":
          case "name":
          case "children":
            break;
          default:
            errs.add(new Violation(k, "unknown field"));
        }
      }

      if (!errs.isEmpty()) {
        throw new ValidationException(p, errs);
      }
      return new Category(Objects.requireNonNull(id), Objects.requireNonNull(name), children);
    }
  }

  public static final class Serializer extends JsonSerializer<Category> {
    @Override
    public void serialize(Category v, JsonGenerator gen, SerializerProvider sp) throws IOException {
      List<Violation> errs = new ArrayList<>();
      if (v.id == null) {
        errs.add(new Violation("id", "required"));
      }
      if (v.name == null) {
        errs.add(new Violation("name", "required"));
      }
      if (!errs.isEmpty()) {
        throw new ValidationException(null, errs);
      }
      gen.writeStartObject();
      gen.writeStringField("id", v.id);
      gen.writeStringField("name", v.name);
      // optional: omit when unset.
      if (v.children != null) {
        gen.writeArrayFieldStart("children");
        for (Category c : v.children) {
          sp.defaultSerializeValue(c, gen); // delegates to Category.Serializer
        }
        gen.writeEndArray();
      }
      gen.writeEndObject();
    }
  }
}
