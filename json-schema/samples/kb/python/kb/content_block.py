# Code generated by nex-gen. DO NOT EDIT.
#
# Source: content_block.json
#
# The root type `Block` declared in this input is part of the cross-file
# Page <-> Block SCC and is hoisted to `_recursive.py` (see
# [[generated-file-layout]] "Recursion: hoist types, not files"). This
# module keeps only the non-cyclic helper `BlockStyle`.

from __future__ import annotations

from typing import Optional

from pydantic import BaseModel, ConfigDict, Field, model_serializer

from .definitions import SpecInt, _emit_set_fields

__all__ = ["BlockStyle"]


class BlockStyle(BaseModel):
    """Presentation styling for a block."""

    model_config = ConfigDict(
        strict=True, populate_by_name=True, extra="forbid"
    )

    bold: Optional[bool] = Field(default=None)
    indent: Optional[SpecInt] = Field(default=None, ge=0)

    @model_serializer(mode="wrap")
    def _serialize(self, handler):
        return _emit_set_fields(self, handler)
