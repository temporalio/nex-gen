// Generated by nex-gen. DO NOT EDIT!

package com.example.chat;

import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.JsonDeserializer;
import com.fasterxml.jackson.databind.JsonNode;
import com.fasterxml.jackson.databind.JsonSerializer;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import org.jspecify.annotations.NullMarked;

/** Closed struct: only the declared members are accepted. */
@NullMarked
@JsonDeserialize(using = SendMessageOutput.Deserializer.class)
@JsonSerialize(using = SendMessageOutput.Serializer.class)
public final class SendMessageOutput {
  private final String messageId;

  public SendMessageOutput(String messageId) {
    this.messageId = messageId;
  }

  public String getMessageId() {
    return messageId;
  }

  public static final class Deserializer extends JsonDeserializer<SendMessageOutput> {
    @Override
    public SendMessageOutput deserialize(JsonParser p, DeserializationContext ctx)
        throws IOException {
      JsonNode root = p.readValueAsTree(); // lenient tree; defeats fail-fast binding
      List<Violation> errs = new ArrayList<>();

      String messageId = null;
      JsonNode n = root.get("messageId");
      if (n == null || n.isNull()) {
        errs.add(new Violation("messageId", "required"));
      } else if (!n.isTextual()) {
        errs.add(new Violation("messageId", "expected string"));
      } else {
        messageId = n.asText();
      }

      // closed struct: any undeclared key is a violation
      Iterator<String> it = root.fieldNames();
      while (it.hasNext()) {
        String k = it.next();
        if (!k.equals("messageId")) {
          errs.add(new Violation(k, "unknown field"));
        }
      }

      if (!errs.isEmpty()) {
        throw new ValidationException(p, errs);
      }
      return new SendMessageOutput(messageId);
    }
  }

  public static final class Serializer extends JsonSerializer<SendMessageOutput> {
    @Override
    public void serialize(SendMessageOutput v, JsonGenerator gen, SerializerProvider sp)
        throws IOException {
      List<Violation> errs = new ArrayList<>();
      if (v.messageId == null) {
        errs.add(new Violation("messageId", "required"));
      }
      if (!errs.isEmpty()) {
        throw new ValidationException(null, errs); // fail before emitting a byte
      }
      gen.writeStartObject();
      gen.writeStringField("messageId", v.messageId);
      gen.writeEndObject();
    }
  }
}
