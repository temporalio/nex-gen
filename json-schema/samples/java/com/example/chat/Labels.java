// Generated by nex-gen. DO NOT EDIT!

package com.example.chat;

import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.JsonDeserializer;
import com.fasterxml.jackson.databind.JsonNode;
import com.fasterxml.jackson.databind.JsonSerializer;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Collections;
import java.util.Iterator;
import java.util.LinkedHashMap;
import java.util.List;
import java.util.Map;
import org.jspecify.annotations.NullMarked;

/**
 * Arbitrary string key/value labels. A pure typed map is still emitted as a
 * named wrapper (not a bare {@code Map}) so adding declared properties later
 * never changes this type's kind.
 */
@NullMarked
@JsonDeserialize(using = Labels.Deserializer.class)
@JsonSerialize(using = Labels.Serializer.class)
public final class Labels {
  private static final int MAX_PROPERTIES = 50;

  private final Map<String, String> additionalProperties;

  public Labels(Map<String, String> additionalProperties) {
    this.additionalProperties = additionalProperties;
  }

  public Map<String, String> getAdditionalProperties() {
    return Collections.unmodifiableMap(additionalProperties);
  }

  public static final class Deserializer extends JsonDeserializer<Labels> {
    @Override
    public Labels deserialize(JsonParser p, DeserializationContext ctx) throws IOException {
      JsonNode root = p.readValueAsTree();
      List<Violation> errs = new ArrayList<>();
      Map<String, String> map = new LinkedHashMap<>();

      Iterator<Map.Entry<String, JsonNode>> it = root.fields();
      while (it.hasNext()) {
        Map.Entry<String, JsonNode> e = it.next();
        JsonNode val = e.getValue();
        if (!val.isTextual()) {
          errs.add(new Violation(e.getKey(), "expected string"));
        } else {
          map.put(e.getKey(), val.asText());
        }
      }

      // count is over distinct wire keys
      if (map.size() > MAX_PROPERTIES) {
        errs.add(new Violation("", "maxProperties: at most " + MAX_PROPERTIES + " entries"));
      }

      if (!errs.isEmpty()) {
        throw new ValidationException(p, errs);
      }
      return new Labels(map);
    }
  }

  public static final class Serializer extends JsonSerializer<Labels> {
    @Override
    public void serialize(Labels v, JsonGenerator gen, SerializerProvider sp) throws IOException {
      if (v.additionalProperties.size() > MAX_PROPERTIES) {
        throw new ValidationException(
            null,
            List.of(new Violation("", "maxProperties: at most " + MAX_PROPERTIES + " entries")));
      }
      gen.writeStartObject();
      for (Map.Entry<String, String> e : v.additionalProperties.entrySet()) {
        gen.writeStringField(e.getKey(), e.getValue());
      }
      gen.writeEndObject();
    }
  }
}
