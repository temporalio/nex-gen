// Generated by nex-gen. DO NOT EDIT!

package com.example.kb;

import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.JsonDeserializer;
import com.fasterxml.jackson.databind.JsonNode;
import com.fasterxml.jackson.databind.JsonSerializer;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Objects;
import org.jspecify.annotations.NullMarked;
import org.jspecify.annotations.Nullable;

/**
 * A page. One half of the Page &lt;-&gt; Block cross-file cycle. Closed struct.
 * The cross-file cycle is free in Java — {@code blocks} is a plain {@link
 * Block} object reference, resolved within the single package.
 */
@NullMarked
@JsonDeserialize(using = Page.Deserializer.class)
@JsonSerialize(using = Page.Serializer.class)
public final class Page {
  private final String pageId;
  private final String title;
  private final PageMeta meta; // required nested object
  private final @Nullable List<Block> blocks; // optional cross-file array

  public Page(String pageId, String title, PageMeta meta, @Nullable List<Block> blocks) {
    this.pageId = pageId;
    this.title = title;
    this.meta = meta;
    this.blocks = blocks;
  }

  public String getPageId() {
    return pageId;
  }

  public String getTitle() {
    return title;
  }

  public PageMeta getMeta() {
    return meta;
  }

  public @Nullable List<Block> getBlocks() {
    return blocks;
  }

  public static final class Deserializer extends JsonDeserializer<Page> {
    @Override
    public Page deserialize(JsonParser p, DeserializationContext ctx) throws IOException {
      JsonNode root = p.readValueAsTree();
      List<Violation> errs = new ArrayList<>();

      String pageId = null;
      JsonNode idNode = root.get("pageId");
      if (idNode == null || idNode.isNull()) {
        errs.add(new Violation("pageId", "required"));
      } else if (!idNode.isTextual()) {
        errs.add(new Violation("pageId", "expected string"));
      } else {
        pageId = idNode.asText();
      }

      String title = null;
      JsonNode titleNode = root.get("title");
      if (titleNode == null || titleNode.isNull()) {
        errs.add(new Violation("title", "required"));
      } else if (!titleNode.isTextual()) {
        errs.add(new Violation("title", "expected string"));
      } else {
        title = titleNode.asText();
      }

      // meta: required $ref to PageMeta -- delegate to its own (de)serializer.
      PageMeta meta = null;
      JsonNode metaNode = root.get("meta");
      if (metaNode == null || metaNode.isNull()) {
        errs.add(new Violation("meta", "required"));
      } else {
        try {
          meta = ctx.readTreeAsValue(metaNode, PageMeta.class);
        } catch (ValidationException nested) {
          for (Violation v : nested.getViolations()) {
            errs.add(new Violation("meta." + v.getPath(), v.getReason()));
          }
        }
      }

      // blocks: optional array of Block (cross-file $ref).
      List<Block> blocks = null;
      JsonNode blocksNode = root.get("blocks");
      if (blocksNode != null && !blocksNode.isNull()) {
        if (!blocksNode.isArray()) {
          errs.add(new Violation("blocks", "expected array"));
        } else {
          blocks = new ArrayList<>();
          for (int i = 0; i < blocksNode.size(); i++) {
            JsonNode item = blocksNode.get(i);
            try {
              blocks.add(ctx.readTreeAsValue(item, Block.class));
            } catch (ValidationException nested) {
              for (Violation v : nested.getViolations()) {
                errs.add(new Violation("blocks[" + i + "]." + v.getPath(), v.getReason()));
              }
            }
          }
        }
      }

      Iterator<String> it = root.fieldNames();
      while (it.hasNext()) {
        String k = it.next();
        switch (k) {
          case "pageId":
          case "title":
          case "meta":
          case "blocks":
            break;
          default:
            errs.add(new Violation(k, "unknown field"));
        }
      }

      if (!errs.isEmpty()) {
        throw new ValidationException(p, errs);
      }
      return new Page(
          Objects.requireNonNull(pageId),
          Objects.requireNonNull(title),
          Objects.requireNonNull(meta),
          blocks);
    }
  }

  public static final class Serializer extends JsonSerializer<Page> {
    @Override
    public void serialize(Page v, JsonGenerator gen, SerializerProvider sp) throws IOException {
      List<Violation> errs = new ArrayList<>();
      if (v.pageId == null) {
        errs.add(new Violation("pageId", "required"));
      }
      if (v.title == null) {
        errs.add(new Violation("title", "required"));
      }
      if (v.meta == null) {
        errs.add(new Violation("meta", "required"));
      }
      if (!errs.isEmpty()) {
        throw new ValidationException(null, errs);
      }
      gen.writeStartObject();
      gen.writeStringField("pageId", v.pageId);
      gen.writeStringField("title", v.title);
      gen.writeFieldName("meta");
      sp.defaultSerializeValue(v.meta, gen); // delegates to PageMeta.Serializer
      // optional: omit when unset.
      if (v.blocks != null) {
        gen.writeArrayFieldStart("blocks");
        for (Block b : v.blocks) {
          sp.defaultSerializeValue(b, gen); // delegates to Block.Serializer
        }
        gen.writeEndArray();
      }
      gen.writeEndObject();
    }
  }
}
