{
  "name": "samples-typescript",
  "private": true,
  "type": "module",
  "scripts": {
    "build-outputs": "node build_outputs.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@temporalio/common": "^1.16.2",
    "nexus-rpc": "^0.0.2"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vitest": "^4.1.0"
  },
  "engines": {
    "node": ">=26"
  }
}
