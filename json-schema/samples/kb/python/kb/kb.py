# Code generated by nex-gen. DO NOT EDIT.
#
# Source: kb.nexusrpc.yaml

from __future__ import annotations

import nexusrpc
from nexusrpc import Operation
from pydantic import BaseModel, ConfigDict, Field, model_serializer

from ._recursive import Block, Page
from .definitions import SpecInt, _emit_set_fields
from .tree_category import Category

__all__ = [
    "KnowledgeBaseService",
    "GetPageInput",
    "PutBlockOutput",
    "GetCategoryTreeInput",
]

# ---------------------------------------------------------------------------
# Service binding.
# ---------------------------------------------------------------------------


@nexusrpc.service(name="example.kb.v1.KnowledgeBaseService")
class KnowledgeBaseService:
    """Fetch and update pages, blocks, and the category tree."""

    get_page: Operation[GetPageInput, Page] = Operation(name="GetPage")
    """Fetch a page by id."""

    put_block: Operation[Block, PutBlockOutput] = Operation(name="PutBlock")
    """Create or update a content block."""

    get_category_tree: Operation[GetCategoryTreeInput, Category] = Operation(
        name="GetCategoryTree"
    )
    """Fetch the category tree rooted at a category."""


# ---------------------------------------------------------------------------
# Synthesized operation I/O types.
# ---------------------------------------------------------------------------


class GetPageInput(BaseModel):
    # Synthesized from the inline `getPage.input` object.
    model_config = ConfigDict(
        strict=True, populate_by_name=True, extra="forbid"
    )

    page_id: str = Field(alias="pageId")

    @model_serializer(mode="wrap")
    def _serialize(self, handler):
        return _emit_set_fields(self, handler)


class PutBlockOutput(BaseModel):
    # Synthesized from the inline `putBlock.output` object.
    model_config = ConfigDict(
        strict=True, populate_by_name=True, extra="forbid"
    )

    block_id: str = Field(alias="blockId")
    revision: SpecInt = Field()

    @model_serializer(mode="wrap")
    def _serialize(self, handler):
        return _emit_set_fields(self, handler)


class GetCategoryTreeInput(BaseModel):
    # Synthesized from the inline `getCategoryTree.input` object.
    model_config = ConfigDict(
        strict=True, populate_by_name=True, extra="forbid"
    )

    root_id: str = Field(alias="rootId")

    @model_serializer(mode="wrap")
    def _serialize(self, handler):
        return _emit_set_fields(self, handler)
