// <auto-generated />
// Generated by nex-gen. DO NOT EDIT!
#nullable enable
#pragma warning disable CS1591

using System;
using System.CodeDom.Compiler;
using System.Collections.Generic;
using System.Text.Json;
using System.Text.Json.Serialization;
using NexGen.Generated.Content.Block;

namespace NexGen.Generated.Content.Page
{

    /// <summary>
    /// A page. One half of the Page &lt;-&gt; Block cross-file cycle. Because the cycle spans two input files, Page and Block hoist together into Python's _recursive.py; the non-cyclic PageMeta helper stays in this module.
    /// </summary>
    [GeneratedCode("nex-gen", null)]
    public class Page : IJsonOnDeserialized
    {
        public Page(string pageId, string title, global::NexGen.Generated.Content.Page.PageMeta meta)
        {
            PageId = pageId;
            Title = title;
            Meta = meta;
        }

        [JsonPropertyName("pageId")]
        [JsonRequired]
        public string PageId { get; init; }
        [JsonPropertyName("title")]
        [JsonRequired]
        public string Title { get; init; }
        [JsonPropertyName("meta")]
        [JsonRequired]
        public global::NexGen.Generated.Content.Page.PageMeta Meta { get; init; }
        /// <summary>
        /// Ordered content blocks. Cross-file `$ref` to block.json (same directory); the array is the terminating edge of the cycle.
        /// </summary>
        [JsonIgnore]
        public IReadOnlyList<global::NexGen.Generated.Content.Block.Block>? Blocks
        {
            get => ReadOptionalValue<List<global::NexGen.Generated.Content.Block.Block>?>("blocks");
            init
            {
                RejectNull("blocks", value);
                AdditionalProperties["blocks"] = value;
            }
        }

        [JsonExtensionData]
        public Dictionary<string, object?> AdditionalProperties { get; set; } = new Dictionary<string, object?>();

        void IJsonOnDeserialized.OnDeserialized()
        {
            foreach (var key in AdditionalProperties.Keys)
            {
                if (key != "blocks")
                {
                    throw new JsonException($"Unknown field `{key}`.");
                }
            }
            if (AdditionalProperties.TryGetValue("blocks", out var blocksValue))
            {
                RejectNull("blocks", blocksValue);
                _ = ReadJsonValue<List<global::NexGen.Generated.Content.Block.Block>?>(blocksValue);
            }
        }

        private T? ReadOptionalValue<T>(string name, T? defaultValue = default)
        {
            if (!AdditionalProperties.TryGetValue(name, out var value))
            {
                return defaultValue;
            }
            return ReadJsonValue<T>(value);
        }

        private static T? ReadJsonValue<T>(object? value)
        {
            if (value is null)
            {
                return default;
            }
            if (typeof(T) == typeof(long?) || typeof(T) == typeof(long))
            {
                return (T?)(object?)ReadJsonInteger(value);
            }
            if (value is JsonElement json)
            {
                return json.Deserialize<T>();
            }
            if (value is T typed)
            {
                return typed;
            }
            return (T)value;
        }

        private static long? ReadJsonInteger(object? value)
        {
            const double maxSafeInteger = 9007199254740991d;
            if (value is null)
            {
                return default;
            }
            double number;
            if (value is JsonElement json)
            {
                if (json.ValueKind == JsonValueKind.Null)
                {
                    return default;
                }
                if (json.ValueKind != JsonValueKind.Number)
                {
                    throw new JsonException("expected integer");
                }
                number = json.GetDouble();
            }
            else if (value is long longValue)
            {
                number = longValue;
            }
            else if (value is int intValue)
            {
                number = intValue;
            }
            else
            {
                throw new JsonException("expected integer");
            }
            if (double.IsNaN(number) || double.IsInfinity(number) || Math.Truncate(number) != number || Math.Abs(number) > maxSafeInteger)
            {
                throw new JsonException("expected integer");
            }
            return (long)number;
        }

        private static void RejectNull(string name, object? value)
        {
            if (value is null || value is JsonElement { ValueKind: JsonValueKind.Null })
            {
                throw new JsonException($"{name}: explicit null not allowed");
            }
        }
    }


    /// <summary>
    /// Non-cyclic helper. Referenced only by Page, references nothing recursive, so it stays in the content_page module even though Page is hoisted.
    /// </summary>
    [GeneratedCode("nex-gen", null)]
    public class PageMeta : IJsonOnDeserialized
    {
        public PageMeta(string author)
        {
            Author = author;
        }

        [JsonPropertyName("author")]
        [JsonRequired]
        public string Author { get; init; }
        [JsonIgnore]
        public long? WordCount
        {
            get => ReadOptionalValue<long?>("wordCount");
            init
            {
                RejectNull("wordCount", value);
                AdditionalProperties["wordCount"] = value;
            }
        }

        [JsonExtensionData]
        public Dictionary<string, object?> AdditionalProperties { get; set; } = new Dictionary<string, object?>();

        void IJsonOnDeserialized.OnDeserialized()
        {
            foreach (var key in AdditionalProperties.Keys)
            {
                if (key != "wordCount")
                {
                    throw new JsonException($"Unknown field `{key}`.");
                }
            }
            if (AdditionalProperties.TryGetValue("wordCount", out var wordCountValue))
            {
                RejectNull("wordCount", wordCountValue);
                _ = ReadJsonValue<long?>(wordCountValue);
            }
        }

        private T? ReadOptionalValue<T>(string name, T? defaultValue = default)
        {
            if (!AdditionalProperties.TryGetValue(name, out var value))
            {
                return defaultValue;
            }
            return ReadJsonValue<T>(value);
        }

        private static T? ReadJsonValue<T>(object? value)
        {
            if (value is null)
            {
                return default;
            }
            if (typeof(T) == typeof(long?) || typeof(T) == typeof(long))
            {
                return (T?)(object?)ReadJsonInteger(value);
            }
            if (value is JsonElement json)
            {
                return json.Deserialize<T>();
            }
            if (value is T typed)
            {
                return typed;
            }
            return (T)value;
        }

        private static long? ReadJsonInteger(object? value)
        {
            const double maxSafeInteger = 9007199254740991d;
            if (value is null)
            {
                return default;
            }
            double number;
            if (value is JsonElement json)
            {
                if (json.ValueKind == JsonValueKind.Null)
                {
                    return default;
                }
                if (json.ValueKind != JsonValueKind.Number)
                {
                    throw new JsonException("expected integer");
                }
                number = json.GetDouble();
            }
            else if (value is long longValue)
            {
                number = longValue;
            }
            else if (value is int intValue)
            {
                number = intValue;
            }
            else
            {
                throw new JsonException("expected integer");
            }
            if (double.IsNaN(number) || double.IsInfinity(number) || Math.Truncate(number) != number || Math.Abs(number) > maxSafeInteger)
            {
                throw new JsonException("expected integer");
            }
            return (long)number;
        }

        private static void RejectNull(string name, object? value)
        {
            if (value is null || value is JsonElement { ValueKind: JsonValueKind.Null })
            {
                throw new JsonException($"{name}: explicit null not allowed");
            }
        }
    }

}
