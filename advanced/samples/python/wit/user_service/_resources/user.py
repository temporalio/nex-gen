# Generated by nexgen v0.2.1. DO NOT EDIT!

from __future__ import annotations

import dataclasses
import typing

from ..models import UpdateEmailRequest


@dataclasses.dataclass
class User:
    user_id: str
    email: str

    async def update_email(
        self,
        email: str,
    ) -> User:
        request = UpdateEmailRequest(
            user_id=self.user_id,
            email=email,
        )
        nexus_client = getattr(self, "_nexus_client")
        handle = await nexus_client.start_operation(
            operation="UpdateEmail",
            input=request,
            output_type=User,
        )
        resource = await handle
        return bind_user_client(
            resource,
            nexus_client,
        )


def bind_user_client(
    resource: User,
    nexus_client: typing.Any,
) -> User:
    setattr(resource, "_nexus_client", nexus_client)
    return resource
