# Code generated by nex-gen. DO NOT EDIT.
#
# Source: tree_category.json

from __future__ import annotations

from typing import Optional

from pydantic import BaseModel, ConfigDict, Field, model_serializer

from .definitions import _emit_set_fields

__all__ = ["Category", "Palette"]


class Category(BaseModel):
    """A category node in the tree."""

    # Within-file self-cycle (Category -> children -> Category): stays in
    # this module, not hoisted to _recursive.py. The `children` back-edge is
    # a string forward annotation; Category.model_rebuild() below resolves it.
    model_config = ConfigDict(
        strict=True, populate_by_name=True, extra="forbid"
    )

    id: str = Field()
    name: str = Field()
    children: Optional[list["Category"]] = Field(default=None)

    @model_serializer(mode="wrap")
    def _serialize(self, handler):
        return _emit_set_fields(self, handler)


class Palette(BaseModel):
    """A named set of color swatches. (Dead $def: unreferenced but exported.)"""

    model_config = ConfigDict(
        strict=True, populate_by_name=True, extra="forbid"
    )

    swatches: list[str] = Field()

    @model_serializer(mode="wrap")
    def _serialize(self, handler):
        return _emit_set_fields(self, handler)


# Resolve the self-cycle forward reference (Category.children).
Category.model_rebuild()
