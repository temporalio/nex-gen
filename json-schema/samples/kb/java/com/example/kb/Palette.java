// Generated by nex-gen. DO NOT EDIT!

package com.example.kb;

import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.JsonDeserializer;
import com.fasterxml.jackson.databind.JsonNode;
import com.fasterxml.jackson.databind.JsonSerializer;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Objects;
import org.jspecify.annotations.NullMarked;

/**
 * A dead {@code $def} — defined but never referenced anywhere. Still generated
 * and exported as intended reusable API surface. Closed struct.
 */
@NullMarked
@JsonDeserialize(using = Palette.Deserializer.class)
@JsonSerialize(using = Palette.Serializer.class)
public final class Palette {
  private final List<String> swatches; // required array of string

  public Palette(List<String> swatches) {
    this.swatches = swatches;
  }

  public List<String> getSwatches() {
    return swatches;
  }

  public static final class Deserializer extends JsonDeserializer<Palette> {
    @Override
    public Palette deserialize(JsonParser p, DeserializationContext ctx) throws IOException {
      JsonNode root = p.readValueAsTree();
      List<Violation> errs = new ArrayList<>();

      // swatches: required array of string.
      List<String> swatches = null;
      JsonNode swatchesNode = root.get("swatches");
      if (swatchesNode == null || swatchesNode.isNull()) {
        errs.add(new Violation("swatches", "required"));
      } else if (!swatchesNode.isArray()) {
        errs.add(new Violation("swatches", "expected array"));
      } else {
        swatches = new ArrayList<>();
        for (int i = 0; i < swatchesNode.size(); i++) {
          JsonNode item = swatchesNode.get(i);
          if (!item.isTextual()) {
            errs.add(new Violation("swatches[" + i + "]", "expected string"));
          } else {
            swatches.add(item.asText());
          }
        }
      }

      Iterator<String> it = root.fieldNames();
      while (it.hasNext()) {
        String k = it.next();
        if (!k.equals("swatches")) {
          errs.add(new Violation(k, "unknown field"));
        }
      }

      if (!errs.isEmpty()) {
        throw new ValidationException(p, errs);
      }
      return new Palette(Objects.requireNonNull(swatches));
    }
  }

  public static final class Serializer extends JsonSerializer<Palette> {
    @Override
    public void serialize(Palette v, JsonGenerator gen, SerializerProvider sp) throws IOException {
      if (v.swatches == null) {
        throw new ValidationException(null, List.of(new Violation("swatches", "required")));
      }
      gen.writeStartObject();
      gen.writeArrayFieldStart("swatches");
      for (String s : v.swatches) {
        gen.writeString(s);
      }
      gen.writeEndArray();
      gen.writeEndObject();
    }
  }
}
