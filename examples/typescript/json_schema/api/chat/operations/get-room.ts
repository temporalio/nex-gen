// Generated by nex-gen. DO NOT EDIT!

import * as workflow from "@temporalio/workflow";
import { chatService } from "../service";
import type { GetRoomInput, Room } from "../models";

/**
 * Look up a room by id.
 *
 * @param endpoint - Endpoint for the service.
 * @param request - Request for the operation.
 */
export async function getRoom(
  endpoint: string,
  request: GetRoomInput,
): Promise<workflow.NexusOperationHandle<Room>> {
  const client = workflow.createNexusServiceClient({
    service: chatService,
    endpoint: endpoint,
  });
  return await client.startOperation(chatService.operations.getRoom, request);
}
