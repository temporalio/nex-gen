// Generated by nex-gen. DO NOT EDIT!

package com.example.kb;

import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.JsonDeserializer;
import com.fasterxml.jackson.databind.JsonNode;
import com.fasterxml.jackson.databind.JsonSerializer;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import org.jspecify.annotations.NullMarked;

/**
 * Synthesized from the inline {@code input} of the {@code getPage} operation.
 * Closed struct.
 */
@NullMarked
@JsonDeserialize(using = GetPageInput.Deserializer.class)
@JsonSerialize(using = GetPageInput.Serializer.class)
public final class GetPageInput {
  private final String pageId;

  public GetPageInput(String pageId) {
    this.pageId = pageId;
  }

  public String getPageId() {
    return pageId;
  }

  public static final class Deserializer extends JsonDeserializer<GetPageInput> {
    @Override
    public GetPageInput deserialize(JsonParser p, DeserializationContext ctx) throws IOException {
      JsonNode root = p.readValueAsTree();
      List<Violation> errs = new ArrayList<>();

      String pageId = null;
      JsonNode n = root.get("pageId");
      if (n == null || n.isNull()) {
        errs.add(new Violation("pageId", "required"));
      } else if (!n.isTextual()) {
        errs.add(new Violation("pageId", "expected string"));
      } else {
        pageId = n.asText();
      }

      Iterator<String> it = root.fieldNames();
      while (it.hasNext()) {
        String k = it.next();
        if (!k.equals("pageId")) {
          errs.add(new Violation(k, "unknown field"));
        }
      }

      if (!errs.isEmpty()) {
        throw new ValidationException(p, errs);
      }
      return new GetPageInput(pageId);
    }
  }

  public static final class Serializer extends JsonSerializer<GetPageInput> {
    @Override
    public void serialize(GetPageInput v, JsonGenerator gen, SerializerProvider sp)
        throws IOException {
      if (v.pageId == null) {
        throw new ValidationException(null, List.of(new Violation("pageId", "required")));
      }
      gen.writeStartObject();
      gen.writeStringField("pageId", v.pageId);
      gen.writeEndObject();
    }
  }
}
