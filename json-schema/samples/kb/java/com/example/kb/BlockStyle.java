// Generated by nex-gen. DO NOT EDIT!

package com.example.kb;

import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.JsonDeserializer;
import com.fasterxml.jackson.databind.JsonNode;
import com.fasterxml.jackson.databind.JsonSerializer;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import org.jspecify.annotations.NullMarked;
import org.jspecify.annotations.Nullable;

/** Non-cyclic helper. All members optional. Closed struct. */
@NullMarked
@JsonDeserialize(using = BlockStyle.Deserializer.class)
@JsonSerialize(using = BlockStyle.Serializer.class)
public final class BlockStyle {
  private final @Nullable Boolean bold; // optional boolean
  private final @Nullable Long indent; // optional integer, minimum 0

  public BlockStyle(@Nullable Boolean bold, @Nullable Long indent) {
    this.bold = bold;
    this.indent = indent;
  }

  public @Nullable Boolean getBold() {
    return bold;
  }

  public @Nullable Long getIndent() {
    return indent;
  }

  public static final class Deserializer extends JsonDeserializer<BlockStyle> {
    @Override
    public BlockStyle deserialize(JsonParser p, DeserializationContext ctx) throws IOException {
      JsonNode root = p.readValueAsTree();
      List<Violation> errs = new ArrayList<>();

      // bold: optional boolean.
      Boolean bold = null;
      JsonNode boldNode = root.get("bold");
      if (boldNode != null && !boldNode.isNull()) {
        if (!boldNode.isBoolean()) {
          errs.add(new Violation("bold", "expected boolean"));
        } else {
          bold = boldNode.asBoolean();
        }
      }

      // indent: optional integer with minimum 0.
      Long indent = null;
      JsonNode indentNode = root.get("indent");
      if (indentNode != null && !indentNode.isNull()) {
        indent = SpecNumbers.specLong(indentNode, "indent", errs);
        if (indent != null && indent < 0) {
          errs.add(new Violation("indent", "minimum: must be >= 0"));
        }
      }

      Iterator<String> it = root.fieldNames();
      while (it.hasNext()) {
        String k = it.next();
        if (!k.equals("bold") && !k.equals("indent")) {
          errs.add(new Violation(k, "unknown field"));
        }
      }

      if (!errs.isEmpty()) {
        throw new ValidationException(p, errs);
      }
      return new BlockStyle(bold, indent);
    }
  }

  public static final class Serializer extends JsonSerializer<BlockStyle> {
    @Override
    public void serialize(BlockStyle v, JsonGenerator gen, SerializerProvider sp)
        throws IOException {
      List<Violation> errs = new ArrayList<>();
      if (v.indent != null && Math.abs(v.indent) > SpecNumbers.CAP) {
        errs.add(new Violation("indent", "integer exceeds +/-(2^53-1) cap"));
      }
      if (v.indent != null && v.indent < 0) {
        errs.add(new Violation("indent", "minimum: must be >= 0"));
      }
      if (!errs.isEmpty()) {
        throw new ValidationException(null, errs);
      }
      gen.writeStartObject();
      // optional: omit when unset.
      if (v.bold != null) {
        gen.writeBooleanField("bold", v.bold);
      }
      if (v.indent != null) {
        gen.writeNumberField("indent", v.indent);
      }
      gen.writeEndObject();
    }
  }
}
