// Generated by nex-gen. DO NOT EDIT!

import * as workflow from "@temporalio/workflow";
import { chatService } from "../services";

/**
 * Liveness probe.
 *
 * @param endpoint - Endpoint for the service.
 */
export async function pingRequest(
  endpoint: string,
): Promise<workflow.NexusOperationHandle<void>> {
  const client = workflow.createNexusServiceClient({
    service: chatService,
    endpoint: endpoint,
  });
  return await client.startOperation(chatService.operations.ping, undefined);
}
