// Generated by nex-gen. DO NOT EDIT!

package com.example.chat;

import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.JsonDeserializer;
import com.fasterxml.jackson.databind.JsonNode;
import com.fasterxml.jackson.databind.JsonSerializer;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import org.jspecify.annotations.NullMarked;

/**
 * Synthesized from the inline {@code input} of the {@code getRoom} operation.
 * Closed struct.
 */
@NullMarked
@JsonDeserialize(using = GetRoomInput.Deserializer.class)
@JsonSerialize(using = GetRoomInput.Serializer.class)
public final class GetRoomInput {
  private final String roomId;

  public GetRoomInput(String roomId) {
    this.roomId = roomId;
  }

  public String getRoomId() {
    return roomId;
  }

  public static final class Deserializer extends JsonDeserializer<GetRoomInput> {
    @Override
    public GetRoomInput deserialize(JsonParser p, DeserializationContext ctx) throws IOException {
      JsonNode root = p.readValueAsTree();
      List<Violation> errs = new ArrayList<>();

      String roomId = null;
      JsonNode n = root.get("roomId");
      if (n == null || n.isNull()) {
        errs.add(new Violation("roomId", "required"));
      } else if (!n.isTextual()) {
        errs.add(new Violation("roomId", "expected string"));
      } else {
        roomId = n.asText();
      }

      Iterator<String> it = root.fieldNames();
      while (it.hasNext()) {
        String k = it.next();
        if (!k.equals("roomId")) {
          errs.add(new Violation(k, "unknown field"));
        }
      }

      if (!errs.isEmpty()) {
        throw new ValidationException(p, errs);
      }
      return new GetRoomInput(roomId);
    }
  }

  public static final class Serializer extends JsonSerializer<GetRoomInput> {
    @Override
    public void serialize(GetRoomInput v, JsonGenerator gen, SerializerProvider sp)
        throws IOException {
      if (v.roomId == null) {
        throw new ValidationException(null, List.of(new Violation("roomId", "required")));
      }
      gen.writeStartObject();
      gen.writeStringField("roomId", v.roomId);
      gen.writeEndObject();
    }
  }
}
