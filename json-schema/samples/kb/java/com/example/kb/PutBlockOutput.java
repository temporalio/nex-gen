// Generated by nex-gen. DO NOT EDIT!

package com.example.kb;

import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.JsonDeserializer;
import com.fasterxml.jackson.databind.JsonNode;
import com.fasterxml.jackson.databind.JsonSerializer;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Objects;
import org.jspecify.annotations.NullMarked;

/**
 * Synthesized from the inline {@code output} of the {@code putBlock} operation.
 * Closed struct.
 */
@NullMarked
@JsonDeserialize(using = PutBlockOutput.Deserializer.class)
@JsonSerialize(using = PutBlockOutput.Serializer.class)
public final class PutBlockOutput {
  private final String blockId;
  private final long revision; // required integer

  public PutBlockOutput(String blockId, long revision) {
    this.blockId = blockId;
    this.revision = revision;
  }

  public String getBlockId() {
    return blockId;
  }

  public long getRevision() {
    return revision;
  }

  public static final class Deserializer extends JsonDeserializer<PutBlockOutput> {
    @Override
    public PutBlockOutput deserialize(JsonParser p, DeserializationContext ctx) throws IOException {
      JsonNode root = p.readValueAsTree();
      List<Violation> errs = new ArrayList<>();

      String blockId = null;
      JsonNode idNode = root.get("blockId");
      if (idNode == null || idNode.isNull()) {
        errs.add(new Violation("blockId", "required"));
      } else if (!idNode.isTextual()) {
        errs.add(new Violation("blockId", "expected string"));
      } else {
        blockId = idNode.asText();
      }

      // revision: required integer.
      Long revision = null;
      JsonNode revNode = root.get("revision");
      if (revNode == null || revNode.isNull()) {
        errs.add(new Violation("revision", "required"));
      } else {
        revision = SpecNumbers.specLong(revNode, "revision", errs);
      }

      Iterator<String> it = root.fieldNames();
      while (it.hasNext()) {
        String k = it.next();
        if (!k.equals("blockId") && !k.equals("revision")) {
          errs.add(new Violation(k, "unknown field"));
        }
      }

      if (!errs.isEmpty()) {
        throw new ValidationException(p, errs);
      }
      return new PutBlockOutput(Objects.requireNonNull(blockId), Objects.requireNonNull(revision));
    }
  }

  public static final class Serializer extends JsonSerializer<PutBlockOutput> {
    @Override
    public void serialize(PutBlockOutput v, JsonGenerator gen, SerializerProvider sp)
        throws IOException {
      List<Violation> errs = new ArrayList<>();
      if (v.blockId == null) {
        errs.add(new Violation("blockId", "required"));
      }
      if (Math.abs(v.revision) > SpecNumbers.CAP) {
        errs.add(new Violation("revision", "integer exceeds +/-(2^53-1) cap"));
      }
      if (!errs.isEmpty()) {
        throw new ValidationException(null, errs);
      }
      gen.writeStartObject();
      gen.writeStringField("blockId", v.blockId);
      gen.writeNumberField("revision", v.revision);
      gen.writeEndObject();
    }
  }
}
