// Generated by nex-gen. DO NOT EDIT!

package com.example.kb;

import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.JsonDeserializer;
import com.fasterxml.jackson.databind.JsonNode;
import com.fasterxml.jackson.databind.JsonSerializer;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Objects;
import org.jspecify.annotations.NullMarked;
import org.jspecify.annotations.Nullable;

/**
 * A content block. The other half of the Page &lt;-&gt; Block cross-file cycle.
 * Closed struct. The {@code page} back-reference is optional + nullable, which
 * terminates the cycle; in Java it is simply a nullable {@link Page} field.
 */
@NullMarked
@JsonDeserialize(using = Block.Deserializer.class)
@JsonSerialize(using = Block.Serializer.class)
public final class Block {
  private final String blockId;
  private final long order; // required integer, minimum 0
  private final @Nullable String text; // optional
  private final @Nullable BlockStyle style; // optional $ref
  private final @Nullable Page page; // optional + nullable back-ref (closes the cycle)

  public Block(
      String blockId,
      long order,
      @Nullable String text,
      @Nullable BlockStyle style,
      @Nullable Page page) {
    this.blockId = blockId;
    this.order = order;
    this.text = text;
    this.style = style;
    this.page = page;
  }

  public String getBlockId() {
    return blockId;
  }

  public long getOrder() {
    return order;
  }

  public @Nullable String getText() {
    return text;
  }

  public @Nullable BlockStyle getStyle() {
    return style;
  }

  /** The containing page, if linked. */
  public @Nullable Page getPage() {
    return page;
  }

  public static final class Deserializer extends JsonDeserializer<Block> {
    @Override
    public Block deserialize(JsonParser p, DeserializationContext ctx) throws IOException {
      JsonNode root = p.readValueAsTree();
      List<Violation> errs = new ArrayList<>();

      String blockId = null;
      JsonNode idNode = root.get("blockId");
      if (idNode == null || idNode.isNull()) {
        errs.add(new Violation("blockId", "required"));
      } else if (!idNode.isTextual()) {
        errs.add(new Violation("blockId", "expected string"));
      } else {
        blockId = idNode.asText();
      }

      // order: required integer with minimum 0.
      Long order = null;
      JsonNode orderNode = root.get("order");
      if (orderNode == null || orderNode.isNull()) {
        errs.add(new Violation("order", "required"));
      } else {
        order = SpecNumbers.specLong(orderNode, "order", errs);
        if (order != null && order < 0) {
          errs.add(new Violation("order", "minimum: must be >= 0"));
        }
      }

      // text: optional string.
      String text = null;
      JsonNode textNode = root.get("text");
      if (textNode != null && !textNode.isNull()) {
        if (!textNode.isTextual()) {
          errs.add(new Violation("text", "expected string"));
        } else {
          text = textNode.asText();
        }
      }

      // style: optional $ref to BlockStyle.
      BlockStyle style = null;
      JsonNode styleNode = root.get("style");
      if (styleNode != null && !styleNode.isNull()) {
        try {
          style = ctx.readTreeAsValue(styleNode, BlockStyle.class);
        } catch (ValidationException nested) {
          for (Violation v : nested.getViolations()) {
            errs.add(new Violation("style." + v.getPath(), v.getReason()));
          }
        }
      }

      // page: optional + nullable back-ref. Absent -> null; explicit null ->
      // accepted as null; a value -> parsed as Page (closes the cycle).
      Page page = null;
      JsonNode pageNode = root.get("page");
      if (pageNode != null && !pageNode.isNull()) {
        try {
          page = ctx.readTreeAsValue(pageNode, Page.class);
        } catch (ValidationException nested) {
          for (Violation v : nested.getViolations()) {
            errs.add(new Violation("page." + v.getPath(), v.getReason()));
          }
        }
      }

      Iterator<String> it = root.fieldNames();
      while (it.hasNext()) {
        String k = it.next();
        switch (k) {
          case "blockId":
          case "order":
          case "text":
          case "style":
          case "page":
            break;
          default:
            errs.add(new Violation(k, "unknown field"));
        }
      }

      if (!errs.isEmpty()) {
        throw new ValidationException(p, errs);
      }
      return new Block(
          Objects.requireNonNull(blockId), Objects.requireNonNull(order), text, style, page);
    }
  }

  public static final class Serializer extends JsonSerializer<Block> {
    @Override
    public void serialize(Block v, JsonGenerator gen, SerializerProvider sp) throws IOException {
      List<Violation> errs = new ArrayList<>();
      if (v.blockId == null) {
        errs.add(new Violation("blockId", "required"));
      }
      if (Math.abs(v.order) > SpecNumbers.CAP) {
        errs.add(new Violation("order", "integer exceeds +/-(2^53-1) cap"));
      }
      if (v.order < 0) {
        errs.add(new Violation("order", "minimum: must be >= 0"));
      }
      if (!errs.isEmpty()) {
        throw new ValidationException(null, errs);
      }
      gen.writeStartObject();
      gen.writeStringField("blockId", v.blockId);
      gen.writeNumberField("order", v.order);
      // optional: omit when unset.
      if (v.text != null) {
        gen.writeStringField("text", v.text);
      }
      if (v.style != null) {
        gen.writeFieldName("style");
        sp.defaultSerializeValue(v.style, gen); // delegates to BlockStyle.Serializer
      }
      // optional+nullable object: omit when unset (conservative collapse, Java).
      if (v.page != null) {
        gen.writeFieldName("page");
        sp.defaultSerializeValue(v.page, gen); // delegates to Page.Serializer
      }
      gen.writeEndObject();
    }
  }
}
